# URL of your deployed realtime backend (the Socket.IO server).
# Local dev:
#   VITE_BACKEND_URL=http://localhost:8080
# Production (your Render/Railway/Fly URL):
#   VITE_BACKEND_URL=https://courtlink-server.onrender.com
VITE_BACKEND_URL=http://localhost:8080

# Only needed for GitHub Pages project sites (https://user.github.io/REPO/).
# Set to "/REPO/". Leave unset for Vercel/Netlify root hosting.
# VITE_BASE=/courtlink/
