{
  "name": "courtlink-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CourtLink Pickleball realtime server (Socket.IO, authoritative simulation).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "socket.io": "^4.7.5",
    "tsx": "^4.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}